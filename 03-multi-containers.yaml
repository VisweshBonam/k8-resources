apiVersion: v1
kind: Pod
metadata:
  name: multi-containers-demo
  labels:
    check: "multi-containers running or not"
spec:
  containers:
  - name: nginx
    image: nginx
  
  - name: almalinux
    image: almalinux:9